Impossible de upload la version finale ainsi que toutes les versions a partir de l'intégration du PDF car il y a trop de fichiers, nous utilisons Vendor et on ne peux pas Upload le dossier donc le site ne marcherais pas si on l'upload sans.
Nous avons donc utilisé un DRIVE pour s'envoyer tout de même nos fichiers sur un espace collaboratif. 
